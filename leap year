
#include <stdio.h>

int main()
{
    int x;
    printf("Enter the year :");
    scanf("%d",&x);
    if(x%4==0){
        printf("%d is a leap year",x);
    }
    else{
        printf("%d is a not leap year",x);
    }


    return 0;
}
