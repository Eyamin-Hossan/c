#include<stdio.h>
#include<math.h>
int main(){

    int base,p;

    printf("enter the base :");
    scanf("%d",&base);

    printf("enter the power :");
    scanf("%d",&p);

    int power = pow(base,p);

    printf("%d", power);

    return 0;

}
